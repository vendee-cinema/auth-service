version: '3.9'

services:
  auth-service:
    container_name: auth-service
    build:
      context: .
      dockerfile: .dockerfile
    restart: always
    env_file:
      - .env.production.local
    networks:
      - vendee-cinema

networks:
  vendee-cinema:
    external: true
